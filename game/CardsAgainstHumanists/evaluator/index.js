// evaluate data from clients.
// returns null if the data is invalid,
// and the new state if it's valid
const evaluator = (currentState, actionData) => {
  const { data, sid } = actionData;

  return currentState;
};

module.exports = evaluator;
